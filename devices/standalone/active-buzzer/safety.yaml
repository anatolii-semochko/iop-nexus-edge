# Always empty for a standalone device (AGENTS.md section 30) - it has no
# forbidden-state rule with anything else since it doesn't share a Node
# with any other device (unlike Heater/Cooler on example-thermal-node,
# whose cross-device rule lives on the Node's own safety.yaml instead).
forbidden: []
