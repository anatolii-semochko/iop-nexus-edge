# Changelog

## 2026-07-26

- Initial version: single `Buzzer` resource (`Bool`), ordinary actuator,
  driven AUTO by the new `active-buzzer` process kind. No physical
  hardware yet - CAN -> STM32 -> Port0/1 planned once ordered parts arrive.
