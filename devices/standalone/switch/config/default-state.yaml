# Initial simulated value. Seeded into apps/device-service's Virtual Node
# Runtime via the "initial.Switch" protocol property - see
# apps/device-service/res/devices/example-devices.yaml - kept here too as
# this device type's own declared default (AGENTS.md section 7).
Switch: false
