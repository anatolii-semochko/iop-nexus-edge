# Forbidden-state rules for this device type, consumed by the central
# Model State Validator (apps/api/src/validator.ts) - see AGENTS.md
# section 7/30/32. Always empty - a cross-device interlock rule is a
# property of a physical assembly a node type pairs together, not of any
# one Device type in isolation; this file exists anyway so every device
# type has the same predictable shape.
forbidden: []
